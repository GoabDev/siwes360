import { FeaturePlaceholder } from "@/components/ui/feature-placeholder";

export default function AdminAdministratorsPage() {
  return (
    <FeaturePlaceholder
      eyebrow="Administrators"
      title="Administrator management moved to super admin"
      description="Department administrators can no longer manage platform administrator records from this workspace. Use the super admin area for global account administration."
    />
  );
}
