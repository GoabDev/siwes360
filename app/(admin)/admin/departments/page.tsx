import { FeaturePlaceholder } from "@/components/ui/feature-placeholder";

export default function AdminDepartmentsPage() {
  return (
    <FeaturePlaceholder
      eyebrow="Departments"
      title="Department management moved to super admin"
      description="Department creation and administrator assignment now live in the super admin workspace so the permission model matches the backend."
    />
  );
}
